<h1><%= @restaurant.name %> </h1>
<p>Category: <%= @restaurant.category %></p>
<p>Adress: <%= @restaurant.address %></p>
<p>Phone number: <%= @restaurant.phone_number %></p>

<span class="m-2"><%= link_to "Edit restaurant", edit_restaurant_path(@restaurant) %></span>
<span class="m-2"><%= link_to  "Delete restaurant", restaurant_path(@restaurant), method: :delete, data: { confirm: 'Are you sure?' } %></span>
<span class="m-2"><%= link_to  "Back to list", restaurants_path %></span>

<h3 class="mt-4">Reviews</h3>
<ul>
  <% @reviews.each do |review| %>
    <li>
      <%= review.content %>
      <%= review.rating %>
    </li>
  <% end %>
</ul>


<p>
  <%= link_to 'Add Review', new_restaurant_review_path(@restaurant), class: 'btn btn-success' %>
</p>